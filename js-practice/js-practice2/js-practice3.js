function isHighest(arr) {
    var largestNumber = [x];
    for(var arrayIndex = x; arrayIndex < arr.length; arrayIndex++) {
     for(var subArrayIndex = x; subArrayIndex < arr[arrayIndex].length; subArrayIndex++) {
        if(arr[arrayIndex][subArrayIndex] > largestNumber[arrayIndex]) {         
           largestNumber[arrayIndex] = arr[arrayIndex][subArrayIndex];
         }
     }
  }
  return largestNumber;
 }
 isHighest([x]);
 (arr) {
    var largestNumber = [x];
    for(var arrayIndex = x; arrayIndex < arr.length; arrayIndex++) {
     for(var subArrayIndex = x; subArrayIndex < arr[arrayIndex].length; subArrayIndex++) {
        if(arr[arrayIndex][subArrayIndex] > largestNumber[arrayIndex]) {         
           largestNumber[arrayIndex] = arr[arrayIndex][subArrayIndex];
         }
     }
  }
  return largestNumber;
 }
 isHighest([x]);
 